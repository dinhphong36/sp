<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

<style>
    body {
        background: linear-gradient(135deg, #f0f4f8, #d9e2ec);
        font-family: 'Segoe UI', sans-serif;
    }

    .app {
        max-width: 500px;
        margin: 50px auto;
        padding: 30px;
        background: white;
        border-radius: 20px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .form-group label {
        font-weight: 600;
        margin-bottom: 8px;
    }

    .form-control {
        border-radius: 12px;
        padding: 10px 15px;
    }

    .btn-primary {
        width: 100%;
        padding: 10px;
        font-size: 16px;
        border-radius: 12px;
        background: linear-gradient(135deg, #4f46e5, #3b82f6);
        border: none;
        transition: background 0.3s ease;
    }

    .btn-primary:hover {
        background: linear-gradient(135deg, #4338ca, #2563eb);
    }

    img {
        display: block;
        margin: 15px auto;
        border-radius: 12px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }
</style>

<div class="app">
    <form method="POST" action="/admin/{{mit._id}}?_method=PUT">
        <div class="form-group mb-3">
            <label for="name">Name</label>
            <input type="text" class="form-control" id="name" name="name" value="{{mit.name}}">
        </div>
        <div class="text-center">
            <img src="{{mit.image}}" width="100px" height="100px" alt="Preview">
        </div>
        <div class="form-group mb-3">
            <label for="image">Image URL</label>
            <input type="text" class="form-control" id="image" name="image" value="{{mit.image}}">
        </div>
        <div class="form-group mb-3">
            <label for="price">Price</label>
            <input type="number" class="form-control" id="price" name="price" value="{{mit.price}}">
        </div>
        <div class="form-group mb-4">
            <label for="info">Info</label>
            <input type="text" class="form-control" id="info" name="info" value="{{mit.info}}">
        </div>
        <button type="submit" class="btn btn-primary">💾 Cập nhật sản phẩm</button>
    </form>
</div>
