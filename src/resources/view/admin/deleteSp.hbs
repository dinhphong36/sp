<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Xoá sản phẩm</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <style>
    .delete-card {
      max-width: 600px;
      margin: 40px auto;
      padding: 30px;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      border-left: 5px solid #dc3545;
    }
    .delete-title {
      font-size: 1.5rem;
      font-weight: bold;
      color: #dc3545;
      margin-bottom: 20px;
    }
    .delete-img {
      width: 120px;
      height: 120px;
      object-fit: cover;
      border-radius: 12px;
      border: 2px solid #dee2e6;
      margin-bottom: 15px;
    }
    .form-control[disabled] {
      background-color: #f8f9fa;
      font-weight: 500;
    }
    .btn-delete {
      background-color: #dc3545;
      border: none;
    }
    .btn-delete:hover {
      background-color: #c82333;
    }
  </style>
</head>
<body class="bg-light">

  <div class="container">
    <div class="delete-card">
      <h2 class="delete-title"><i class="fas fa-trash-alt me-2"></i>Xác nhận xoá sản phẩm</h2>

      <form method="POST" action="/admin/{{mit._id}}/deleted?_method=DELETE">
        <div class="mb-3">
          <label class="form-label">Tên sản phẩm</label>
          <input type="text" class="form-control" value="{{mit.name}}" disabled />
        </div>

        <div class="text-center">
          <img src="{{mit.image}}" alt="Ảnh sản phẩm" class="delete-img" />
        </div>

        <div class="mb-3">
          <label class="form-label">Link ảnh</label>
          <input type="text" class="form-control" value="{{mit.image}}" disabled />
        </div>

        <div class="mb-3">
          <label class="form-label">Giá</label>
          <input type="number" class="form-control" value="{{mit.price}}" disabled />
        </div>

        <div class="mb-3">
          <label class="form-label">Thông tin</label>
          <textarea class="form-control" rows="3" disabled>{{mit.info}}</textarea>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/admin/updateSp" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left me-1"></i> Quay lại
          </a>
          <button type="submit" class="btn btn-delete">
            <i class="fas fa-trash me-1"></i> Xác nhận xoá
          </button>
        </div>
      </form>
    </div>
  </div>

</body>
</html>
